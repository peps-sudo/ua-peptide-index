# Ukraine Peptide Price Index — методологія (v2, alpha)

**Що це.** Відкритий індекс цін на research-grade пептиди в Україні, у гривнях за 1 мг.
Оновлюється з публічних вітрин магазинів, які агрегує [peps.co.ua](https://peps.co.ua).
Ліцензія **CC-BY-4.0** — використовуйте вільно з посиланням на джерело.

> ⚠️ **ALPHA.** Метод агрегації (v2) перероблено, щоб прибрати частотне зміщення (див. нижче), але
> замір поки підтверджено на **2 молекулах із 24** (ретатрутид, тирзепатид). Для решти числа
> **попередні**. Формальний валідований реліз — після заміру на ≥500 SKU. Розділ «Відкриті
> обмеження» нижче — чесний.

> Це НЕ медична порада і НЕ реклама лікарських засобів. Дані — фактичні ціни з відкритих вітрин.
> Жодних вигаданих чи оцінних цифр: якщо числа немає в реальній вітрині, його немає в індексі.

## Джерело й одиниця
- Джерело: історія цін каталогу peps.co.ua (та сама, що живить сайт).
- Одиниця: **грн за 1 мг** (`грн_за_флакон / доза_в_мг`).

## Метод: агрегація по СТАНУ, не по подіях (v2)
Раніше медіана рахувалася по подіях зміни ціни — і магазин, що оновлює ціну частіше, важив
більше (частотне зміщення). У v2 цього немає:

1. **Один стан на магазин на день.** Для кожного магазину на кожен день беремо одну ціну — його
   поточний стан (carry-forward: жива вітрина тягне останній відомий стан до сьогодні; делістнутий
   магазин — лише до останнього спостереження, `in_stock` tombstone).
2. **Ціна магазину = його найдешевша грн/мг** (мінімум по дозах цього магазину).
3. **Поверх магазинів** публікуємо `p25 / median / p75` — типова ціна й діапазон ринку на дату.
4. Рядок за день публікується лише за наявності **≥2 магазинів** (інакше перцентиль недостовірний).

## Що виключено і чому
1. **Зареєстровані лікарські засоби (пени)** — Mounjaro, Ozempic, Wegovy, Zepbound, Saxenda,
   Rybelsus і бренди-виробники. Аптечний пен коштує в рази більше за мг — інша категорія.
2. **Неправдоподібні зчитування.** Ціна поза межами правдоподібності молекули (канон
   `scrape_price_band`, межі збережено в `meta.plausibility_bands`) — це парс-баг: подію
   **ігноруємо**, стан магазину лишається попереднім (ряд не рветься).
3. **«Ціна за запитом»** та позиції без коректної ціни за мг.

## Відкриті обмеження (чесно — alpha)
- Валідація зниження зміщення підтверджена на **2 молекулах** (ретатрутид, тирзепатид), не на 500 SKU.
- Стабільні 30–68 днів ціни можуть нести реальний ринковий тренд, не лише артефакт методу.
- Шляхи `unit='mg'` / `price_on_request` не в вході (переважно дублі, заміряно).
- До повної публікації дозамірюємо: `Δ(до−після)` на ≥500 SKU + кореляцію «вік стану ↔ відхилення».

## Обмеження (загальні)
- Це ціни вітрин, не транзакцій; фактична ціна продажу може відрізнятися.
- Покриття залежить від того, які магазини ми скрапимо; це не весь ринок України.
- «research-grade» — за позиціонуванням магазину, не за незалежним лабораторним підтвердженням партії.
- Округлення до цілих грн/мг.

## Файли
- `data/ua-peptide-price-history-weekly.json` — денна історія по молекулі (`p25/median/p75`, obs).
- `data/ua-peptide-price-index-current.csv` / `.json` — останній стан по кожній молекулі (плоский зріз).
- Обидва — з ОДНОГО v2-джерела (один метод). Розбивку по дозах тимчасово знято до узгодження її методу.

## Як цитувати
> Ukraine Peptide Price Index (v2, alpha), peps.co.ua, <дата>. CC-BY-4.0. https://peps.co.ua/price-index

## Відтворюваність
Генерується детермінованим скриптом-експортером (лише читання БД, без ручних правок чисел). Кожен
фільтр підписаний причиною. Індекс не містить жодного числа, введеного руками.
